export enum ExamType {
    import = "import",
    auto = "auto",
    user = "user",
}

export enum ExamLangType {
    en = "en",
    vi = "vi",
}

export enum uploadExamType {
    multiple = "multiple",
    single = "single",
}